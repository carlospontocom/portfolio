name: Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      # 🔍 DIAGNÓSTICO - Verifica o ambiente
      - name: Debug environment
        run: |
          echo "📦 Node version:"
          node --version
          echo ""
          echo "📦 NPM version:"
          npm --version
          echo ""
          echo "📦 Scripts disponíveis:"
          cat package.json | grep -A 10 '"scripts"'
          echo ""
          echo "📦 Dependências instaladas:"
          npm list --depth=0 || echo "⚠️ Erro ao listar dependências"
          echo ""
          echo "📁 Conteúdo da raiz:"
          ls -la
      
      # 🔧 GARANTE A INSTALAÇÃO DO TAILWIND E VITE
      - name: Ensure Tailwind and Vite are installed
        run: |
          echo "🔍 Verificando instalação do Tailwind..."
          npm list tailwindcss @tailwindcss/vite || echo "⚠️ Tailwind não encontrado, instalando..."
          npm install tailwindcss @tailwindcss/vite --save-dev
          echo "✅ Tailwind instalado:"
          npm list tailwindcss @tailwindcss/vite
      
      # 🔑 Cria o .env com os Secrets
      - name: Create .env file
        run: |
          echo "VITE_FIREBASE_API_KEY=${{ secrets.VITE_FIREBASE_API_KEY }}" >> .env
          echo "VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}" >> .env
          echo "VITE_FIREBASE_PROJECT_ID=${{ secrets.VITE_FIREBASE_PROJECT_ID }}" >> .env
          echo "VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}" >> .env
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}" >> .env
          echo "VITE_FIREBASE_APP_ID=${{ secrets.VITE_FIREBASE_APP_ID }}" >> .env
          echo "VITE_FIREBASE_MEASUREMENT_ID=${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}" >> .env
          echo "VITE_EMAILJS_SERVICE_ID=${{ secrets.VITE_EMAILJS_SERVICE_ID }}" >> .env
          echo "VITE_EMAILJS_TEMPLATE_ID=${{ secrets.VITE_EMAILJS_TEMPLATE_ID }}" >> .env
          echo "VITE_EMAILJS_PUBLIC_KEY=${{ secrets.VITE_EMAILJS_PUBLIC_KEY }}" >> .env
      
      # 🔍 Verifica se o .env foi criado
      - name: Check .env file
        run: |
          echo "🔍 Verificando .env:"
          if [ -f ".env" ]; then
            echo "✅ .env criado com sucesso!"
            echo "📋 Variáveis definidas:"
            cat .env | cut -d'=' -f1
          else
            echo "❌ .env NÃO foi criado!"
            exit 1
          fi
      
      - name: Build project
        run: npm run build
      
      # 🔍 DIAGNÓSTICO - Verifica o build
      - name: Debug build output
        run: |
          echo "🔍 Verificando se a pasta dist foi criada:"
          if [ -d "dist" ]; then
            echo "✅ dist existe!"
            echo "📁 Arquivos gerados no build:"
            ls -la dist/
            echo ""
            echo "📄 Verificando CSS:"
            if ls dist/assets/*.css 2>/dev/null; then
              echo "✅ CSS encontrado!"
              ls -la dist/assets/*.css
            else
              echo "❌ NENHUM CSS encontrado!"
            fi
            echo ""
            echo "📄 Verificando JS:"
            if ls dist/assets/*.js 2>/dev/null; then
              echo "✅ JS encontrado!"
              ls -la dist/assets/*.js
            else
              echo "❌ NENHUM JS encontrado!"
            fi
            echo ""
            echo "📄 Primeiras 30 linhas do index.html:"
            head -30 dist/index.html
          else
            echo "❌ dist NÃO existe!"
            echo "📁 Conteúdo da raiz:"
            ls -la
            exit 1
          fi
      
      - name: Force new deployment
        run: echo "Deployed at $(date +%s)" > dist/version.txt
      
      # Tenta o deploy uma vez
      - name: Deploy to Firebase Hosting (attempt 1)
        uses: FirebaseExtended/action-hosting-deploy@v0.10.0
        id: deploy1
        continue-on-error: true
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO }}'
          projectId: app-vuets
          channelId: live
      
      # Se falhar, tenta novamente com uma nova versão
      - name: Retry deployment with new version
        if: steps.deploy1.outcome == 'failure'
        run: |
          echo "Retry at $(date +%s)" >> dist/version.txt
          echo "Retry deployment..."
      
      - name: Deploy to Firebase Hosting (attempt 2)
        if: steps.deploy1.outcome == 'failure'
        uses: FirebaseExtended/action-hosting-deploy@v0.10.0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO }}'
          projectId: app-vuets
          channelId: live
